import { useDeck } from "./use-deck";
import { useDecks } from "./use-decks";
import { useDeckCard } from "./use-deck-card";
import { useDeckCards } from "./use-deck-cards";
const decks = {
  useDeck,
  useDecks,
  useDeckCard,
  useDeckCards,
};

export default decks;
